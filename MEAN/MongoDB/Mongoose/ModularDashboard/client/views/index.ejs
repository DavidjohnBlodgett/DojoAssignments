<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Mongoose Dashboard Landing Page</title>
    </head>
    <body>
        <h1>All Cats</h1>
        <% for(var i in cats){ %>
            <% var read_url = `/cats/${cats[i]._id}`%>
            <% var update_url = `/cats/edit/${cats[i]._id}`%>
            <% var delete_url = `/cats/destroy/${cats[i]._id}`%>
            <p>Name: <a href=<%= read_url%>><%=cats[i].name%></a> Color: <%=cats[i].color%> Age: <%=cats[i].age%></p>
            <form class="" action=<%= update_url%> method="get">
                <button type="submit" name="button">EDIT</button>
            </form>
            <form class="" action=<%= delete_url%> method="post">
                <button type="submit" name="button">DELETE</button>
            </form>
        <% } %>
        <a href="/cats/new">ADD A CAT!</a>
    </body>
</html>
